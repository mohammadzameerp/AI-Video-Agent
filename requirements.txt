# ── AI Video Assistant ── requirements.txt ────────────────────────────────────

# ── Audio / Video acquisition ─────────────────────────────────────────────────
# audioop-lts restores the `audioop` module removed in Python 3.13+ (needed by pydub)
audioop-lts>=0.2.1
# Primary YouTube path: fetch captions directly — no download, no 403 blocks
youtube-transcript-api>=0.6.1
yt-dlp>=2024.4.9
pydub>=0.25.1
ffmpeg-python>=0.2.0

# ── Speech-to-Text ────────────────────────────────────────────────────────────
# faster-whisper: no numba/llvmlite dependency, works on Python 3.13/3.14,
# runs efficiently on CPU with int8 quantization (4x faster than openai-whisper)
faster-whisper>=1.0.0

# ── Translation ────────────────────────────────────────────────────────────────
deep-translator>=1.11.4

# ── LLM Orchestration — LangChain + Mistral ───────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-mistralai>=0.1.0
mistralai>=0.4.0

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
chromadb>=0.5.0
langchain-chroma>=0.1.1
sentence-transformers>=3.0.0
langchain-huggingface>=0.0.3
huggingface-hub>=0.23.0
tiktoken>=0.7.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-extras>=0.4.0
watchdog>=4.0.0

# ── PDF & TXT Export ──────────────────────────────────────────────────────────
reportlab>=4.2.0
fpdf2>=2.7.9

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.26.0
tqdm>=4.66.0
requests>=2.32.0
